// Central source of truth for registration + pricing copy.
// Flip `isOpen` to true (and set `registerHref`) when registration goes live.
export const REGISTRATION = {
  isOpen: false,
  opensLabel: 'Opens July 6th at 5pm',
  opensShort: 'July 6 · 5pm',
  registerHref: '', // add the live registration URL here when it opens
  price: '$500',
  priceUnit: 'per tournament',
  priceLabel: '$500 per tournament',
}
